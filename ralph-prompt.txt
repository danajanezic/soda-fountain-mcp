Iterate on the Socrata MCP server until all 40 stress test questions score an A grade. Each iteration: 1) Run the naive agent test script at tests/agent-stress/naive-agent.mjs using Node.js (remember to set mise PATH and source .env first). 2) Analyze which questions scored below A and identify what the MCP did wrong. 3) Fix the MCP code only in src/ directory files. 4) Rebuild with npm run build. 5) Re-run and check. Critical: DO NOT modify tests/agent-stress/naive-agent.mjs or questions.json. ALL improvements must be MCP server code. If the naive agent picks the wrong dataset, improve search ranking. If it builds a bad query, improve MCP guidance. Run unit tests before committing to avoid regressions. Working dir: /Users/danajanezic/code/or-mcp